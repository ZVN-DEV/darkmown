node_modules/
dist/
.omx/
.DS_Store
*.log
.playwright-mcp/
.vercel
/*.png
screenshots/

# Playwright e2e artifacts (browser binaries live in node_modules / OS cache)
test-results/
playwright-report/
/playwright/.cache

# Local agent worktrees & session state
.claude/

# Agent-generated audit reports, sprint plans & design specs — keep local, never commit
# (repo-wide: catches root and docs/strategy/ etc.).
AUDIT-REPORT.md
SPRINT-PLAN.md
*-REPORT.md
*-PLAN.md
docs/superpowers/
