node_modules/
dist/
.omx/
.DS_Store
*.log
.playwright-mcp/
.vercel
/*.png

# Playwright e2e artifacts (browser binaries live in node_modules / OS cache)
test-results/
playwright-report/
/playwright/.cache

# Local agent worktrees & session state
.claude/

# Agent-generated audit reports & sprint plans — keep local, never commit.
# Root-anchored so intentional docs (e.g. docs/strategy/*) are unaffected.
/AUDIT-REPORT.md
/SPRINT-PLAN.md
/*-REPORT.md
/*-PLAN.md
