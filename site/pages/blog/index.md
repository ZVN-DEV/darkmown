---
title: The Darkmown Blog
description: Notes on shipping Markdown that runs — feeds, drafts, and zero-JS releases.
---

# The Darkmown Blog

Short notes on the framework. Every post here is a plain `.md` file with a
`date:` in its frontmatter — that `date:` is what lands a page in `rss.xml` and
stamps its `<lastmod>` in `sitemap.xml`.

- [Shipping feeds without a config file](/blog/shipping-feeds/)
- [Drafts that never leak](/blog/drafts-that-never-leak/)
