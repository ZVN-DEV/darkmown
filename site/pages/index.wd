---
title: Darkmown — Markdown that runs, not Markdown that needs React
description: A live store widget — search filter, add-to-cart, running total — written as one Markdown file that runs. The same outcome in Markdoc needs a React component, hydration, and a build step. Compare them side by side.
---

@include /nav.wd

<main>

<section class="hero">
  <p class="eyebrow">Darkmown vs. Markdoc</p>

# Markdown that runs. Not Markdown that needs React.

The left file is Markdown. It is also a running app — a live search filter, add-to-cart, and a running total. The right file is the closest Markdoc can get on its own: a static list. To make it do what the left one already does, you add a React component, hydration, and a build step.

<a class="btn primary" href="/app/">See a whole app in one file</a>
<a class="btn" href="/docs/">Read the docs</a>
</section>

<section class="versus">

::: div .vs-card .vs-dark
<header class="vs-head"><span class="vs-tag vs-tag-wd">Darkmown</span><span class="vs-sub">store.wd</span></header>

```wd
:state products = [{"id":1,"name":"Aurora Lamp","price":49},{"id":2,"name":"Briza Fan","price":39},{"id":3,"name":"Cove Speaker","price":89},{"id":4,"name":"Dune Mug","price":12}]
:state cart = []
:state q = ""
:computed count = cart.length

:bind q placeholder="Search products…"

@loop products into p where p.name contains q
::: card .product
**{ p.name }** — ${ p.price }
:button "Add" -> cart += p
:::
@endloop

Cart: { count } item(s)
```

<p class="vs-live-label">↓ and here is that exact file, running</p>

::: div .vs-live
:state products = [{"id":1,"name":"Aurora Lamp","price":49},{"id":2,"name":"Briza Fan","price":39},{"id":3,"name":"Cove Speaker","price":89},{"id":4,"name":"Dune Mug","price":12}]
:state cart = []
:state q = ""
:computed count = cart.length

:bind q placeholder="Search products…"

@loop products into p where p.name contains q
::: card .product
**{ p.name }** — ${ p.price }
:button "Add" -> cart += p
:::
@endloop

Cart: **{ count }** item(s)
:::

<p class="vs-caption">This is the whole file. It runs.</p>
:::

::: div .vs-card
<header class="vs-head"><span class="vs-tag vs-tag-md">Markdoc</span><span class="vs-sub">store.md</span></header>

```text
{% if $products %}
- **Aurora Lamp** — $49
- **Briza Fan** — $39
- **Cove Speaker** — $89
- **Dune Mug** — $12
{% /if %}
```

<p class="vs-live-label">Markdoc has <code>{% if %}</code>, <code>{% partial %}</code>, and <code>$variables</code> — but no loop tag, so each row is written by hand and the list is static. To filter, add to a cart, or total it, you reach for code:</p>

::: div .vs-needs
<strong>+ store-widget.jsx</strong> — a React component, registered as a custom tag:

```jsx
const tags = { store: { render: 'Store' } };
Markdoc.renderers.react(content, React, {
  components: { Store } // your filter + cart + total live here
});
```

<span class="vs-needs-foot">…plus a React + hydration runtime shipped to the browser, and the build step that wires the tag to the component.</span>
:::

<p class="vs-caption">Same outcome needs a React component.</p>
:::

</section>

:note "Static pages ship zero JavaScript. The moment a page asks for reactivity — like this very homepage — one ~4.7 KB gzipped runtime loads (CI-enforced under 5 KB), never before. JS optional, CSS optional, always."

<section class="props">

## Three rules, no escape hatch

- **Zero-JS static.** A page with no state ships no framework JavaScript. `/markdown/` and `/docs/` prove it — open them and check the network tab.
- **One runtime.** Every reactive page shares the same ~4.7 KB gzipped runtime, CI-enforced under 5 KB. No per-component bundles, no hydration tax that scales with your UI.
- **One loop, one binding.** `@loop … into …` is the only loop; `{ name }` is the only interpolation. Nothing else to learn, nothing else to misuse.

</section>

## A whole framework, in text

Everything a site needs, expressed as Markdown directives. No components, no config, no JSX, no special files:

- **Routing** — folders are routes. Drop a file in, it's a page. Prefix with `.` or `-` to hide it.
- **State & reactivity** — `:state`, `:button`, `:if`, and keyed `@loop`s patch the DOM directly. No virtual DOM.
- **Server calls** — `:fetch` pulls JSON into state; `:form action=` round-trips to any backend and degrades to a native POST without JS.
- **Composition** — `@include` embeds one Markdown file into another, passing values down Liquid-style.
- **Styling, optional** — colocate a `.skin` file for design tokens, or ship none and let the browser default.

## What to try

:try "A whole live app in one .wd file: fetch, loops, nested ifs, a form" href="/app/"
:try "The docs route — a .wd page built from includes" href="/docs/"
:try "A plain .md page where directives stay inert text" href="/markdown/"
:try "Reactive page: state, keyed loops, scoped sections" href="/reactive/"
:try "Data page: fetch, forms, server round-trips, persistence" href="/data/"
:try "A hidden route, to confirm it 404s" href="/-draft/"

## Loops that read like prose

`@loop` is the only loop. Point it at a JSON file, an in-scope value, or a `:state` list, and the body repeats once per row. Includes inside the loop inherit the loop value:

```wd
@loop /features.json into card
@include /feature-card.wd
@endloop
```

@loop /features.json into card
@include /feature-card.wd
@endloop

## Colocated behavior

This button is powered by `site/pages/index.js`, discovered by matching the page basename:

<button class="pulse-button" data-wd-demo>Script is waiting</button>

<span id="deploy"></span>

## Deploy in one click

`npm run build` emits a plain static `dist/` — no server, no special runtime — so it deploys anywhere that serves files. This very site is a Darkmown build:

<a class="btn primary" href="https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2FZVN-DEV%2Fdarkmown">Deploy to Vercel</a>
<a class="btn" href="https://deploy.workers.cloudflare.com/?url=https%3A%2F%2Fgithub.com%2FZVN-DEV%2Fdarkmown">Deploy to Cloudflare</a>
<a class="btn" href="https://github.com/ZVN-DEV/darkmown">Star on GitHub</a>

</main>
