---
html: true
---

<!-- Scoped component B. Reuses the SAME class names as scoped-promo.wd
     (.card / .title / .body) but its colocated scoped-stat.skin scopes them to a
     dark, centered stat tile. Proof the leak is gone: two `.card` definitions on
     one page, each only styling its own subtree. -->
<article class="card">
  <span class="title">99.9%</span>
  <p class="body">uptime, because there is no client framework to crash</p>
  <span class="badge">measured</span>
</article>
